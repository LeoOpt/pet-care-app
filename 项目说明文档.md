# 宠物上门喂养小程序 - 项目说明

## 项目概述

这是一个完整的宠物上门喂养微信小程序，采用双端设计模式：
- **下单者（用户端）**：需要预约宠物服务的宠物主人
- **接单者（服务者端）**：提供上门喂养服务的喂养师

## 技术架构

### 核心技术
- **前端框架**：微信小程序原生框架
- **开发语言**：JavaScript、WXML、WXSS、JSON
- **数据存储**：本地存储（wx.setStorageSync / wx.getStorageSync）
- **状态管理**：全局状态管理（app.globalData）

### 项目结构

```
pet-feeding/
├── app.js                 # 全局应用入口
├── app.json               # 全局配置（页面路由、tabBar等）
├── app.wxss               # 全局样式
├── project.config.json    # 项目配置文件
├── sitemap.json          # 站点地图配置
│
├── pages/                 # 下单者端页面
│   ├── home/             # 首页（搜索、服务、喂养师）
│   ├── publish/           # 发布需求（下订单）
│   ├── pets/             # 宠物管理
│   │   ├── pets.js       # 宠物列表页
│   │   ├── add-pet/      # 添加宠物页
│   │   └── edit-pet/     # 编辑宠物页
│   ├── orders/            # 订单列表
│   ├── order-detail/      # 订单详情
│   ├── profile/           # 个人中心
│   └── auth/              # 认证相关
│       └── register-feeder/  # 服务者注册认证
│
├── feeder/                # 接单者端页面
│   └── pages/
│       ├── home/          # 接单首页（可接订单）
│       ├── my-orders/     # 我的订单
│       ├── order-detail/   # 订单详情
│       ├── income/         # 收入统计
│       ├── settings/       # 服务设置
│       └── profile/       # 服务者中心
│
├── components/            # 公共组件（预留）
├── utils/                 # 工具函数（预留）
└── images/               # 图片资源（预留）
```

## 核心功能

### 下单者端功能

#### 1. 首页（[pages/home](file:///e:/code/SOLO_pets/pages/home/home.js)）
- 🔍 **搜索功能**：搜索喂养师或服务
- 🎯 **服务类型入口**：快速选择服务类型
  - 上门喂养
  - 遛狗服务
  - 宠物洗澡
  - 更多服务
- 👤 **热门喂养师推荐**：展示评分高、评价多的喂养师

#### 2. 发布需求（[pages/publish](file:///e:/code/SOLO_pets/pages/publish/publish.js)）
- **步骤1**：选择宠物（支持多选）
- **步骤2**：选择服务类型（多选）
- **步骤3**：选择服务时间
- **步骤4**：确认订单信息
  - 填写服务地址
  - 添加备注
  - 查看费用明细
- **步骤5**：提交订单

#### 3. 宠物管理（[pages/pets](file:///e:/code/SOLO_pets/pages/pets/pets.js)）
- ✅ **添加宠物**：完善宠物信息
  - 基本信息（名称、种类、品种、性别、年龄、体重）
  - 健康信息（是否绝育、疫苗情况、健康状况）
  - 喂养信息（饮食习惯、食量说明、性格特点）
- ✏️ **编辑宠物**：修改宠物信息
- 🗑️ **删除宠物**：移除宠物信息
- ⭐ **设为默认**：设置默认服务宠物

#### 4. 订单管理（[pages/orders](file:///e:/code/SOLO_pets/pages/orders/orders.js)）
- 📋 **订单筛选**：全部/待支付/待接单/进行中/已完成
- 📄 **订单详情**：
  - 订单状态展示
  - 喂养师信息
  - 服务详情
  - 费用明细
- 🔄 **订单操作**：
  - 取消订单
  - 联系喂养师
  - 确认完成
  - 评价服务

#### 5. 个人中心（[pages/profile](file:///e:/code/SOLO_pets/pages/profile/profile.js)）
- 👤 **用户信息**：头像、昵称、手机号
- 📊 **数据统计**：订单数、宠物数、余额
- 🚀 **快捷入口**：发布需求、我的订单、我的宠物
- 🔄 **身份切换**：切换为服务者

### 接单者端功能

#### 1. 接单首页（[feeder/pages/home](file:///e:/code/SOLO_pets/feeder/pages/home/home.js)）
- 💰 **今日收入概览**：今日收入、今日订单、本月收入
- 📋 **可接订单列表**：
  - 订单号、用户信息
  - 服务类型、服务时间
  - 距离、价格
- 🔍 **订单筛选**：全部/附近优先/高收入/各类服务
- ⚡ **一键接单**：批量接受多个订单

#### 2. 我的订单（[feeder/pages/my-orders](file:///e:/code/SOLO_pets/feeder/pages/my-orders/my-orders.js)）
- 📂 **订单分类**：进行中/已完成
- 📄 **订单详情**：
  - 客户信息
  - 服务详情
  - 服务地址
  - 费用明细

#### 3. 收入统计（[feeder/pages/income](file:///e:/code/SOLO_pets/feeder/pages/income/income.js)）
- 📊 **收入概览**：
  - 今日/本周/本月收入
  - 可提现余额
- 💵 **收入明细**：
  - 订单收入列表
  - 订单号、服务类型、金额
- 💸 **提现功能**：
  - 最低提现金额
  - 提现记录查询

#### 4. 服务设置（[feeder/pages/settings](file:///e:/code/SOLO_pets/feeder/pages/settings/settings.js)）
- ⚙️ **服务类型配置**：开启/关闭各项服务
- 💰 **服务价格设置**：设置每项服务的价格
- ⏰ **服务时间设置**：
  - 开始/结束接单时间
  - 预约提前时间
- 📍 **服务区域设置**：设置服务范围

#### 5. 服务者中心（[feeder/pages/profile](file:///e:/code/SOLO_pets/feeder/pages/profile/profile.js)）
- 👤 **账户信息**：头像、姓名、评分
- 📈 **数据统计**：订单数、收入、评分
- 📝 **认证状态**：实名认证状态
- 🔄 **身份切换**：切换为用户

### 服务者注册认证（[pages/auth/register-feeder](file:///e:/code/SOLO_pets/pages/auth/register-feeder/register-feeder.js)）

#### 认证步骤
1. **基本信息**
   - 真实姓名
   - 身份证号码
   - 资质认证（可选）
     - 宠物护理证书
     - 宠物美容证书
     - 兽医资质

2. **身份认证**
   - 上传身份证正面照
   - 上传身份证背面照
   - 审核提示

3. **提交审核**
   - 信息确认
   - 协议同意
   - 提交审核

## 数据管理

### 本地存储数据结构

#### 用户信息
```javascript
{
  userRole: 'user' | 'feeder',
  isFeeder: Boolean,
  pets: [],
  orders: [],
  balance: Number
}
```

#### 宠物信息
```javascript
{
  id: String,
  name: String,
  type: 'cat' | 'dog' | 'other',
  breed: String,
  gender: 'male' | 'female',
  age: Number,
  weight: Number,
  isNeutered: Boolean,
  vaccinationStatus: String,
  healthInfo: String,
  feedingNotes: String,
  behaviorNotes: String,
  isDefault: Boolean
}
```

#### 订单信息
```javascript
{
  id: String,
  orderNo: String,
  userId: String,
  feederId: String,
  status: 'unpaid' | 'pending' | 'processing' | 'completed' | 'cancelled',
  services: [{ name: String, price: Number }],
  pets: [String],
  serviceTime: String,
  serviceAddress: String,
  remark: String,
  totalAmount: Number,
  actualPaid: Number,
  createTime: String
}
```

## 用户身份切换机制

### 切换流程
1. **用户 → 服务者**：
   - 个人中心 → 切换为服务者
   - 完成实名认证
   - 设置服务信息
   - 切换到接单者端 tabBar

2. **服务者 → 用户**：
   - 服务者中心 → 切换为用户
   - 保留服务者身份和设置
   - 切换到下单者端 tabBar

### 实现原理
```javascript
// app.js
App({
  globalData: {
    userRole: 'user',  // 'user' | 'feeder'
    isFeeder: false
  },
  
  switchRole(role) {
    this.globalData.userRole = role;
    this.globalData.isFeeder = (role === 'feeder');
    wx.setStorageSync('userRole', role);
    
    // 切换到对应页面
    const targetPage = role === 'user' 
      ? '/pages/home/home' 
      : '/feeder/pages/home/home';
    wx.reLaunch({ url: targetPage });
  }
})
```

## 页面导航关系图

### 下单者端
```
首页
├── 搜索喂养师
├── 选择服务类型
│   └── 发布需求（4步流程）
├── 点击喂养师卡片
│   └── 订单详情
└── 底部导航
    ├── 首页
    ├── 订单
    │   └── 订单详情
    └── 我的
        ├── 我的订单
        ├── 我的宠物
        │   ├── 添加宠物
        │   └── 编辑宠物
        ├── 我的钱包
        ├── 优惠券
        └── 切换为服务者 → 接单者端
```

### 接单者端
```
接单首页
├── 今日收入概览
├── 可接订单列表
│   ├── 单个接单
│   └── 一键接单
└── 底部导航
    ├── 首页（可接订单）
    ├── 订单（我的订单）
    └── 我的（服务者中心）
        ├── 我的订单
        ├── 收入统计
        ├── 服务设置
        ├── 实名认证
        └── 切换为用户 → 下单者端
```

## 快速开始

### 1. 导入项目
1. 打开微信开发者工具
2. 选择「导入项目」
3. 选择项目目录
4. 填写 AppID（可使用测试号）
5. 点击「导入」

### 2. 测试功能

#### 下单者模式
1. 进入「我的」页面
2. 添加宠物（测试数据已预置）
3. 进入「首页」，点击「发布需求」
4. 完成4步下单流程
5. 在「订单」页面查看订单

#### 接单者模式
1. 进入「我的」页面
2. 点击「切换为服务者」
3. 进入「接单首页」，查看可接订单
4. 点击接单
5. 在「我的订单」查看已接订单

### 3. 体验完整流程
1. **用户下单** → 发布需求 → 填写信息 → 提交订单
2. **切换为服务者** → 接单 → 完成服务
3. **切换回用户** → 确认完成 → 评价服务

## 主题色与设计规范

### 主题色
- **主色**：#FF9D5C（橙色）
- **辅色**：#FFF9F0（暖白色背景）
- **成功色**：#4CAF50（绿色）
- **警告色**：#FF9800（橙色）
- **信息色**：#2196F3（蓝色）

### 设计规范
- **字体**：系统字体
- **圆角**：12rpx - 20rpx
- **阴影**：0 2rpx 10rpx rgba(0, 0, 0, 0.05)
- **间距**：20rpx - 30rpx
- **按钮**：圆角按钮，圆角50rpx

## 后续开发建议

### Phase 1：功能完善
- [ ] 实现真实的微信支付功能
- [ ] 添加消息推送功能
- [ ] 实现即时通讯（聊天功能）
- [ ] 添加地图导航功能

### Phase 2：体验优化
- [ ] 实现完整的评价系统
- [ ] 添加优惠券系统
- [ ] 实现用户等级系统
- [ ] 添加积分商城

### Phase 3：运营支持
- [ ] 添加运营Banner轮播
- [ ] 实现活动推送
- [ ] 添加数据统计后台
- [ ] 实现客服系统

## 常见问题

### Q1: 如何添加真实的图片资源？
A: 在 `images/` 目录下添加图片文件，然后在相应的页面中引用。例如：
```javascript
// pages/home/home.js
avatar: '/images/avatar1.png'
```

### Q2: 如何修改主题色？
A: 修改 `app.wxss` 中的全局样式，以及 `app.json` 中的 navigationBarBackgroundColor。

### Q3: 如何添加新的服务类型？
A: 修改 `pages/publish/publish.js` 中的 serviceTypes 数组。

### Q4: 如何连接后端API？
A: 在 `utils/` 目录下创建 API 请求工具类，替换现有的本地存储逻辑。

## 技术支持

如有问题，请参考：
- 微信小程序官方文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- 项目计划文档：`.trae/documents/完善宠物上门喂养小程序双端功能计划.md`

## 更新日志

### v2.0.0 (2024-01-23)
- ✨ 新增双端用户体系
- ✨ 新增服务者认证流程
- ✨ 新增宠物管理功能
- ✨ 新增收支统计功能
- ✨ 优化订单流程体验
- 🎨 统一设计规范

### v1.0.0 (2024-01-15)
- 🎉 初始版本发布
- ✅ 基础下单功能
- ✅ 订单管理功能
- ✅ 个人中心雏形

---

**© 2024 宠物上门喂养小程序 | 基于微信小程序开发**
